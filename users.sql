CREATE TABLE `naturatech`.`users` (
  `idusers` INT NOT NULL AUTO_INCREMENT,
  `author` VARCHAR(45) NOT NULL,
  `job` VARCHAR(45) NOT NULL,
  `photo` VARCHAR(500) NOT NULL,
  PRIMARY KEY (`idusers`));
